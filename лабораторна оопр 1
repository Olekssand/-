Кнопка інтерфейсу є

	    метод render()
	    метод onClick(f)
	

	клас WindowsButton реалізує Button is
	    метод render(a, b) є
	        // Відобразити кнопку в стилі Windows.
	    Метод onClick(f) є
	        // Навісити на кнопку обробник подій Windows.
	

	клас HTMLButton реалізує Button is
	    метод render(a, b) є
	        // Повернути HTML-код кнопки.
	    Метод onClick(f) є
	        // Навісити на кнопку обробник подій браузера.
	

	

	// Базовий клас фабрики. Зауважте, що "фабрика" — це всього лише
	// додаткова роль для цього класу. Скоріше за все, він уже має
	// якусь бізнес-логіку, яка потребує створення продукту.
	клас Діалог є
	    метод render() є
	        // Щоб використати фабричний метод, ви маєте
	        // пересвідчитися, що ця бізнес-логіка не залежить від
	        // конкретних класів продуктів. Button — це загальний
	        // інтерфейс кнопок, тому все гаразд.
	        Кнопка okButton = createButton()
	        okButton.onClick(closeDialog)
	        okButton.render()
	

	    // Ми виносимо весь код створення продукту до особливого
	    // методу, який називається "фабричним".
	    абстрактний метод createButton():Button
	

	

	// Конкретні фабрики перевизначають фабричний метод і повертають
	// з його власними продуктами.
	клас WindowsDialog розширює діалог
	    метод createButton():Кнопка є
	        повернути нову WindowsButton()
	

	Клас WebDialog розширює діалог
	    метод createButton():Кнопка є
	        повернути новий HTMLButton()
	

	

	Клас Application is
	    діалог поля: Діалог
	

	    // Програма створює певну фабрику в залежності від
	    // конфігурації або оточення.
	    метод initialize() є
	        config = readApplicationConfigFile()
	

	        if (config.OS == "Windows") then
	            діалог = новий WindowsDialog()
	        else if (config.OS == "Web") then
	            діалог = новий WebDialog()
	        інше
	            генерувати новий виняток ("Помилка! Невідома операційна система.")
	

	    // Якщо весь інший клієнтський код працює з фабриками та
	    // продуктами тільки через загальний інтерфейс, то для нього
	    // байдуже, фабрику якого типу було створено на початку.
	    метод main() є
	        this.initialize()
	        dialog.render()

